services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      JWT_SECRET: "change-this-in-production-32-byte-secret"
      ADMIN_USERNAME: "admin"
      ADMIN_PASSWORD: "admin123"
    ports:
      - "8000:8000"
